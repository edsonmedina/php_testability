{{> partials/header }}

<table class="table table-striped table-hover table-condensed">
	<thead>
		<tr>
			<th>Scope</th>
			<th>Status</th>
		</tr>
	</thead>
	<tbody>
		{{#scopes}}
		<tr>
			<td><a href="#">{{name}}()</a></td>
			<td>
				{{#issues}}
				<span class="badge">{{issues}} Issues</span>
				{{/issues}}
				{{^issues}}
				<span class="label label-success">OK</span>
				{{/issues}}
			</td>
		</tr>
		{{/scopes}}
	</tbody>
</table>

<div class="codeline">     1.	foreach ($xbafgasdf as $k) {</div>
<span class="label label-danger">Global function</span>
<div class="codeline bg-danger">     2.		dostuff();</div>
<div class="codeline">     3.	}</div>

{{> partials/footer }}