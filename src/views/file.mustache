{{> partials/header }}

<h4>{{currentPath}} {{#untestable}}<span class="label label-danger">Untestable</span>{{/untestable}}</h4>

<div>&nbsp;</div>

<table class="table table-bordered table-hover table-condensed">
	<tbody>
		<tr>
			<th>Procedures</th>
			<th>Status</th>			
		</tr>	

		{{#scopes}}
		<tr>
			<td><a href="#{{startLine}}">{{name}}</a></td>
			<td>
				{{#issues}}
				<a href="#{{startLine}}" class="btn btn-danger btn-xs" role="button">
				  Issues <span class="badge">{{issues}}</span>
				</a>
				{{/issues}}

				{{^issues}}
				<a href="#{{startLine}}" class="btn btn-success btn-xs" role="button">Testable</a>
				{{/issues}}
			</td>
		</tr>
		{{/scopes}}
		{{^scopes}}
		<tr class="success">
			<td> No issues </td>
			<td><span class="label label-success">OK</span></td>
		</tr>
		{{/scopes}}
	</tbody>
</table>

<div>&nbsp;</div>
<div>&nbsp;</div>

{{#lines}}
{{#issues}}
	<span class="label label-danger">
		<span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span>
		{{type}} {{#name}}({{.}}){{/name}}
	</span> &nbsp;
{{/issues}}
<div class="codeline{{#issues}} bg-danger{{/issues}}"><a name="{{line}}" href="#{{line}}">{{line}}.</a>	{{text}}</div>
{{/lines}}

{{> partials/footer }}
